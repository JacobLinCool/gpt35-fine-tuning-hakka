# GPT-3.5 Fine-Tuning Hakka

## Steps

1. Put `data.csv` into `data/` directory.
2. Configure `.env` file as `env.example` shows.
3. Run `pnpm all` (or `npm run all`).

The intermediate files and results will be stored in `data/` directory.
